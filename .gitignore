# Dependencies
node_modules/
.pnp
.pnp.js

# Env
.env
.env.local
.env.*.local
!.env.example

# Builds
dist/
build/
.next/
out/
*.tsbuildinfo

# Prisma
prisma/*.db
prisma/*.db-journal
# Prisma auto-install artifact at repo root (schema lives at prisma/, consumed by
# backend/ — the real client generates into backend/node_modules). Never imported.
/package.json
/package-lock.json

# Uploads (runtime file storage — keep the dir, ignore contents)
backend/src/uploads/*
!backend/src/uploads/.gitkeep

# Logs
*.log
npm-debug.log*
logs/

# Test / coverage
coverage/
.nyc_output/

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json
